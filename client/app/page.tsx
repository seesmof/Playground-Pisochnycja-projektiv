const baseUrl = "https://open-bible-api.vercel.app/PSA/";

export default async function IndexPage() {
  const chapter = 
  const data = await fetch();

  return <div className="p-3 mx-auto max-w-3xl"></div>;
}
